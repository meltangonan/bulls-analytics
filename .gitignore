# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# Testing
.pytest_cache/
.coverage
htmlcov/

# Output
output/*
!output/.gitkeep

# Cached data. Cheap refetches, the licensed Helvetica extraction DESIGN.md forbids
# committing, and third-party portraits. Anything a published number rests on that is
# slow or rate-limited to rebuild does NOT belong here — it goes to the post's tracked
# docs/visuals/<slug>/data/ folder. See bulls/visuals.py.
cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# direnv / secrets
.envrc
.env

# OS
.DS_Store
Thumbs.db

# Logs
*.log
# Impeccable skill (local/cache state; shared config and critique snapshots are committed)
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/live/sessions/
