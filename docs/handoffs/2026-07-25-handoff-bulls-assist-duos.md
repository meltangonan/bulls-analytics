# CLOSED — Bulls assist-duos posts

**Status: CLOSED** — visual folders shipped; transfer complete.

Which Bulls pairs created the most baskets for one another. One analysis, two visual projects.
The yearly board is the post that went forward; the decade board is parked on Notion.

| Project | Slug | Outcome |
| --- | --- | --- |
| Yearly connections | `assist-duos` | Shipped under `docs/visuals/2026-08-08-assist-duos/` |
| Decade board | `assist-duos-by-decade` | Parked; assets under `docs/visuals/2026-08-08-assist-duos-by-decade/` |

Both render from `scripts/prototypes/assist_duos.py`. Season extracts live in the yearly post's
tracked `docs/visuals/2026-08-08-assist-duos/data/seasons/` (not ignored `cache/`).

Settled ranking, games-together denominator, and the play-by-play identity traps are in
`scripts/prototypes/README.md` and `docs/reference/play-by-play.md`. Last known Notion state when
this closed: `Mocked` for the yearly post, `Parked` for the decade board.
