[pytest]
# pytest-timeout is in requirements.txt for optional local use. Leave the
# timeout unset here: matplotlib render tests can exceed 30s on a cold machine,
# and a global cap would flake rather than catch hangs.
addopts = -v
